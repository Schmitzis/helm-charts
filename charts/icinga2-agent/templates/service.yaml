apiVersion: v1
kind: Service
metadata:
  name: {{ include "icinga2-agent.fullname" . }}
  labels:
    {{- include "icinga2-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "icinga2-agent.selectorLabels" . | nindent 4 }}
